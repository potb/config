--- a/crates/jcode-tui/src/tui/keybind.rs
+++ b/crates/jcode-tui/src/tui/keybind.rs
@@ -524,6 +524,9 @@
     let KeyCode::Char(ch) = code else {
         return None;
     };
+    if std::env::var_os("JCODE_NO_MACOS_OPTION_CHAR_SHORTCUTS").is_some() {
+        return None;
+    }
 
     // macOS terminals that do not treat Option as Meta/Alt insert these Unicode
     // characters for Option+Shift+letter on a US keyboard. Copy badges advertise
--- a/crates/jcode-tui-core/src/keybind.rs
+++ b/crates/jcode-tui-core/src/keybind.rs
@@ -66,6 +66,9 @@
     let KeyCode::Char(ch) = code else {
         return None;
     };
+    if std::env::var_os("JCODE_NO_MACOS_OPTION_CHAR_SHORTCUTS").is_some() {
+        return None;
+    }
 
     match ch {
         'å' => Some('a'),
